[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
invenio-subjects-fast = {editable = true, path = "."}
requests = ">=2.31.0"
halo = ">=0.0.31"
tqdm = ">=4.65.0"
click = ">=8.1.3"
pip-tools = "*"
check-manifest = "*"

[dev-packages]
pytest = ">=7.3.2"
responses = ">=0.23.1"
pyyaml = ">=5.4.1"

[requires]
python_version = ">=3.9"
